name: Basic CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run basic shell
        run: echo "🚀 Workflow ทำงานแล้ว!"
